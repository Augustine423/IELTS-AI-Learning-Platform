apiVersion: v1
kind: Service
metadata:
  name: ielts-ai-backend
  namespace: ielts-ai
  labels:
    app.kubernetes.io/name: ielts-ai-backend
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ielts-ai-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
