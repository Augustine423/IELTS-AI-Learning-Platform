apiVersion: v1
kind: Service
metadata:
  name: ielts-ai-frontend
  namespace: ielts-ai
  labels:
    app.kubernetes.io/name: ielts-ai-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ielts-ai-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
