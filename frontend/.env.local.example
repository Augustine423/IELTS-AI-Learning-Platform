# Leave blank to use the Next.js server-side proxy (/api/backend → backend).
# For local frontend-only testing against a local API, uncomment:
# NEXT_PUBLIC_API_URL=http://localhost:8000
# API_INTERNAL_URL=http://localhost:8000
