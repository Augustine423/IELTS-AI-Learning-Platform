# Optional: direct backend URL for local dev without the Next.js proxy
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend URL for the server-side proxy (set automatically in Docker/k8s)
# API_INTERNAL_URL=http://localhost:8000
